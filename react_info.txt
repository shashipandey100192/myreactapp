react_info
=========================================
1) about reactjs
	1) it's library
	2) work on virtual object
	3) work on  component based structures
	4) create a single application (SPA)
	5) light weight application
	6) made by facebook meta
	7) csr vs ssr

2) files types
	.js
	.jsx
	.ts
	.tsx
	.json
3) work process 
4) types of error
	1) syntax ( text red,background black)
	2) logical( theams, document white)

Note: all html element must have closing tag;
Note: function should have return type
NOte: one function have one return
Note: one return have one element
Note: replace    a 	====== 	Link
      replace 	 href	======	to
      replace	 class	======	className
----------------------------------------------------------
Note: hydration error: object(element dis order)
5)  types of import or export 
	1) name import or export  {}
	2) default import/ export

Note: one module(file) have one default function 
Note:  Parsing error : 	
6) elements wraping (group)
	1) html element (div,section,header,footer...)
	2) <></>	// blank element 
	3) Fragment	// 
------------------------------------------------
7) source path
	1) CDN(online) 
	2) src folder (local)
Note: img elements must have an alt prop
Note: can't write in to alt "image","photo","picture"

8) types of components
	1) function component 
	2) class component
	3) HOC (High order component)
	
9) css link type
	1) inline
	2) external

10) style component
	1) custom html and css 
	2) bootstrap 
	3) talwindcss
	4) MUI
	5) react bootstrap
	
11) JSX  or expression    {}
	string,number,boolean,variable,function,component,element,operation,object,array,condition		
------------------------------------------------------
12) bootstrap (package)(module) 
	1) install package
		1) npm install bootstrap		//install latest version of bootstrap
		2) npm install bootstrap@4.5.0		//install particular version of bootstrap
		3) npm uninstall bootstrap		// uninstall bootstrap
	2) import (index.js) 
		import "bootstrap/dist/css/bootstrap.min.css";
		import "bootstrap/dist/js/bootstrap.bundle.js";
	3) inject
 
13) tailwindcss
	1) npm install tailwindcss @tailwindcss/cli
	2) create input.css inside src folder and import it
		@import "tailwindcss";
	3) open new terminal and run
		npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch
	4) auto generate output.css
	5) import in (index.js)
		import "./output.css"
-----------------------------------------------------
14) icon (react-icons)
	1) npm install react-icons --save
	2) import particular icons
	3) inject 

15) react rouing (navigate)
	1) npm install react-router-dom
	2) import { BrowserRouter, Routes,Route } from 'react-router-dom';
	3) inject 

16) types of routing 
	1) static route
	2) dyn routing
	3) param routing / param query routing 
	4) wild card routing/ not found page / 404 page/ error page
	5) nested routing
	6) moudle routing / lazy loading
------------------------------------------------------
17) git /gitHub
	1) git login 
	2) Create a new repository(repo)
Note: local project name and repo name must have same 
	3) git init 			//initialized git in project  
	4) git remote -v		// check connection(remote)
	5) git remote add origin repopath	// create connection to repo
	 6) git add --all			// add all files on git (added)
	7) git commit -m "any message"		// message for comment 
	8) git push origin branchname		// push data on branch 
	9) git clone pastrepopath		// clone repo 
	10) npm install			// generate node_module (after clone or if install package)
	-------------------
		git add --all
		git commit -m "msg"
		git push origin branchname
		git pull origin branchname
	--------------------------------
	11) git branch branchname		// creae a new branch
	12) git checkout branchname		// switch branch

--------------------------------------------------
18) react input or form validation
	1) custom (core js)
	2) library(package)
		1) npm install react-hook-form
		2) import { useForm } from "react-hook-form"
		3) inject 
		4) validation with error messagae  ( formState: { errors } ) 
			1) single error message
			 	{errors.pass && message}
			2) multi error message
				{errors.pass?.type==="required" && message)

19) react hooks(special function) 
	1) rules of hooks
		1) prefix (use)
		2) can't use inside function or nested function
		3) can't use inside loop
		4) can't use bottom of component
	2) list of hooks
		useState()
		useEffect()
		useRef()
		useMemo()
		useNavigator()
			...	
----------------------------------------------
20) React component lifecycle
Note:Parsing error?

21) types of link
	1) static
		<Link>
	2) dyn 
		useNavigate()
----------------------------------------------------------
NOte: Each child in a list should have a unique "key" prop
-----------------------------------------------------
22) nested routing 
	<Outlet>		// same lik container	
----------------------------------------------------------
23) network cells
	1) fetch (core function)
	2) axios (lib)
 
24) param routing
	1) send id on url
		to={"userdetails/"+c.id}
	2) routing 
		path='users/userdetails/:id'
	3) get id from url
		 const id = useParams()
	4) send id with url
		 fetch(`https://dummyjson.com/users/${id}`)
---------------------------------------------------
25) data share (data pass)
	1) props		// pass data from parent to child
	2) props driling	// parent to child to sub child (not projective)
	3) context API		// group data share
	4) storage		// but can't for sansative data 
	5) redux		// global state management 
26) chart (graph)
	1) npm install recharts
	2) import
	3) inject
	4)  charts type
		1) static
		2) dyn ( based on api) 
------------------------------------------------------------
27) notification (react-toastify) 
	1) npm install react-toastify
	2) import in particular component
	3) inject 
--------------------------------------------------------------

28) json server
	1) npm install json-server
	2) create a file outside src folder "mydatabase.json"
	3) npx json-server mydatabase.json --watch --port 5800 

29) param routing(query param routing)
	1) send id on url
		to={"useredit/"+u.id}
	2) route component
		 <Route path='userlistpage/useredit/:id' element={<Useredit/>}>
	3) get id from url
		const {id} =useParams();
	4) run api 
-------------------------------------------------
30) CRUD
	C: create  / insert
	R: read / view
	U: update / edit
	D: delete / remove

31)  redux 
	1) npm install @reduxjs/toolkit react-redux
	2) create store 
	3) wrap store with application 
		

---------------
configureStore
Provider
reducer
store
createSlice 
useSelector
useSelector/useDispatch 
dispatch 
state
actions
---------------------
	














	
	

	 
 
	






	
